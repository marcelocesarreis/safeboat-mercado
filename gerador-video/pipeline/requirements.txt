# Núcleo do pipeline
anthropic>=0.60
pyyaml>=6.0
requests>=2.31

# Voz local (opcional — backend xtts; requer Python <= 3.11)
# TTS>=0.22

# Os backends de vídeo (Wav2Lip / SadTalker) são repositórios clonados à parte,
# com seus próprios requirements — veja o README.
